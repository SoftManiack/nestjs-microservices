import { Environment } from './types/configurations.enums';
export declare class ApplicationsConfig {
    NODE_ENV: Environment;
    HTTP_HOST: string;
    HTTP_PORT: string;
    HTTP_PREFIX: string;
    HTTP_VERSION: string;
}
